
{{#power-select 
	selected=selectedType
	placeholder="Viewing all"
	searchEnabled=false
	allowClear=true
	options=uniqueMappedProducts
	onchange=(action "selectProductType") 	
as |type|}}
	{{type}}
{{/power-select}}

{{#each dynamicallyFilteredProducts as |p|}}

		{{product-listing product=p}}
	
{{/each}}

<!--
	<div id="slickWrapper">
	 
		{{#slick-slider 
			slidesToShow=5
			responsive=breakpoints
			arrows=false
			autoplay=true
			speed=200
			swipeToSlide=true
			touchThreshold=10
			rows=2
		}}
			
		{{/slick-slider}}			
	</div>

-->