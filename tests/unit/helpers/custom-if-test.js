import { customIf } from 'kwc/helpers/custom-if';
import { module, test } from 'qunit';

module('Unit | Helper | custom if');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = customIf([42]);
  assert.ok(result);
});
